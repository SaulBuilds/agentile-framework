# Agent Hook Templates

> Copy the appropriate file to your project root to ensure your AI agent automatically picks up the Agentile framework.

---

## Claude Code (CLAUDE.md)

Create a file at your project root called `CLAUDE.md`:

```markdown
# CLAUDE.md

IMPORTANT: Before doing ANY work, read `.agentile/AGENT_ENTRY.md` and follow its instructions completely. This project uses the Agentile framework for documentation-first, behavior-driven, test-driven development. The entry file contains a mandatory pre-flight check that must be completed before any other action.

Do not skip initialization. Do not write code before reading the framework rules.

Start here: `.agentile/AGENT_ENTRY.md`
```

---

## Cursor (.cursorrules)

Create a file at your project root called `.cursorrules`:

```
IMPORTANT: Before doing ANY work, read `.agentile/AGENT_ENTRY.md` and follow its instructions completely. This project uses the Agentile framework for documentation-first, behavior-driven, test-driven development. The entry file contains a mandatory pre-flight check that must be completed before any other action.

Do not skip initialization. Do not write code before reading the framework rules.

Start here: `.agentile/AGENT_ENTRY.md`
```

---

## GitHub Copilot (.github/copilot-instructions.md)

Create `.github/copilot-instructions.md`:

```markdown
IMPORTANT: Before doing ANY work, read `.agentile/AGENT_ENTRY.md` and follow its instructions completely. This project uses the Agentile framework for documentation-first, behavior-driven, test-driven development. The entry file contains a mandatory pre-flight check that must be completed before any other action.

Do not skip initialization. Do not write code before reading the framework rules.

Start here: `.agentile/AGENT_ENTRY.md`
```

---

## Windsurf (.windsurfrules)

Create a file at your project root called `.windsurfrules`:

```
IMPORTANT: Before doing ANY work, read `.agentile/AGENT_ENTRY.md` and follow its instructions completely. This project uses the Agentile framework for documentation-first, behavior-driven, test-driven development. The entry file contains a mandatory pre-flight check that must be completed before any other action.

Do not skip initialization. Do not write code before reading the framework rules.

Start here: `.agentile/AGENT_ENTRY.md`
```

---

## Generic (README mention)

If your agent reads README.md automatically, add this to the top:

```markdown
> **AI Agents**: This project uses the [Agentile framework](.agentile/AGENT_ENTRY.md). Read `.agentile/AGENT_ENTRY.md` before doing any work.
```
